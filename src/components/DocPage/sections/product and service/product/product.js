import React from 'react';
import {Trans} from '@lingui/macro';

const PRODUCT_ITEMS = [
    {
        title:<Trans>Product</Trans>,
        image:[null],
        description:<Trans>NOT AVAILABLE!</Trans>
    }
];

export default PRODUCT_ITEMS;